# Python dependencies imported by the swarm Python code.
genesis-world==1.4.1
numpy==2.2.6
opencv-python==4.12.0.88
pyqtgraph==0.14.0
PyQt6==6.9.1

# ROS 1 scripts under drones/ros_ardrone also require these ROS packages,
# installed through the matching ROS distribution rather than pip:
# roslib, rospy, geometry_msgs, std_msgs, nav_msgs